# RupeeRadar — Backend Dependencies
# Python 3.10+

# ── Web Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# ── Data Validation ──
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Database ──
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0
# PostgreSQL (recommended for production on Railway)
# asyncpg>=0.29.0

# ── Data Processing ──
pandas>=2.0.0
openpyxl>=3.1.0

# ── PDF Parsing ──
PyMuPDF>=1.24.0
camelot-py[cv]>=0.11.0  # Optional: requires ghostscript

# ── AI / LLM (Phase 2+) ──
groq>=0.12.0
openai>=1.0.0

# ── Reporting (Phase 3+) ──
jinja2>=3.1.0
weasyprint>=62.0  # Optional: PDF generation

# ── Development ──
python-dotenv>=1.0.0
